using System;
using System.Collections.Generic;
using System.Text;

namespace SimOS
{
    /*
     * Class to read and write from RAM when I/O instruction is encountered.
     * Needs to have some sort of channel, ram address of the data to be
     * transferred and starting address of a RAM buffer in/from data is to
     * be transferred.
     */

    public class DMA
    {
        public DMA()
        {

        }

        public void readData()
        {
        }

        public void writeData()
        {

        }
    }
}
